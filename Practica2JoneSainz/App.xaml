<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Practica2JoneSainz"
             x:Class="Practica2JoneSainz.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <x:Double x:Key="GlobalFontSize">16</x:Double>
            <x:String x:Key="GlobalFontFamily">OpenSansRegular</x:String>
            <Color x:Key="GlobalTextColor">#000000</Color>

            <Style TargetType="Label">
                <Setter Property="FontSize" Value="{DynamicResource GlobalFontSize}" />
                <Setter Property="FontFamily" Value="{DynamicResource GlobalFontFamily}" />
                <Setter Property="TextColor" Value="{DynamicResource GlobalTextColor}" />
            </Style>

            <Style TargetType="Button">
                <Setter Property="FontFamily" Value="{DynamicResource GlobalFontFamily}" />
                <Setter Property="FontSize" Value="{DynamicResource GlobalFontSize}" />
                <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#E1E1E1, Dark=#333333}" />
                <Setter Property="TextColor" Value="{DynamicResource GlobalTextColor}" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="10" />
                <Setter Property="Margin" Value="0,5" />
            </Style>

            <Style x:Key="CardStyle" TargetType="Frame">
                <Setter Property="BorderColor" Value="{DynamicResource GlobalTextColor}" />
                <Setter Property="HasShadow" Value="True" />
                <Setter Property="CornerRadius" Value="15" />
                <Setter Property="Padding" Value="20" />
                <Setter Property="Margin" Value="0,10" />
            </Style>

            <Style TargetType="Entry">
                <Setter Property="TextColor" Value="{DynamicResource GlobalTextColor}" />
                <Setter Property="FontFamily" Value="{DynamicResource GlobalFontFamily}" />
                <Setter Property="FontSize" Value="{DynamicResource GlobalFontSize}" />
                <Setter Property="Margin" Value="0,0,0,15" />
            </Style>

            <Style TargetType="StackLayout">
                <Setter Property="Spacing" Value="6" />
            </Style>
            
            <Style TargetType="Grid">
                <Setter Property="RowSpacing" Value="6" />
                <Setter Property="ColumnSpacing" Value="6" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
